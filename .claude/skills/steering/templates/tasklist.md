# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合
- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

---

## フェーズ1: {フェーズ名}

> **チェックポイント CP-1**: このフェーズ完了後、受け入れ条件を全て確認してから次へ進む。

- [ ] {タスク1}
  - [ ] {サブタスク1-1}
  - [ ] {サブタスク1-2}
  - **Validation**: `{検証コマンドまたは確認手順}`
  - **受け入れ条件**: {何が確認できれば完了か}

- [ ] {タスク2}
  - [ ] {サブタスク2-1}
  - [ ] {サブタスク2-2}
  - **Validation**: `{検証コマンドまたは確認手順}`
  - **受け入れ条件**: {何が確認できれば完了か}

### CP-1 チェックポイント確認

- **完了日時**: {YYYY-MM-DD HH:MM} ← 完了時に記録
- **Evidence（証拠）**: {テスト結果・ログ・スクショの要約}
- **Recovery（復旧メモ）**: {問題が起きた場合の対処内容 / 問題なし}

---

## フェーズ2: {フェーズ名}

> **チェックポイント CP-2**: このフェーズ完了後、受け入れ条件を全て確認してから次へ進む。

- [ ] {タスク1}
  - [ ] {サブタスク1-1}
  - [ ] {サブタスク1-2}
  - **Validation**: `{検証コマンドまたは確認手順}`
  - **受け入れ条件**: {何が確認できれば完了か}

- [ ] {タスク2}
  - **Validation**: `{検証コマンドまたは確認手順}`
  - **受け入れ条件**: {何が確認できれば完了か}

### CP-2 チェックポイント確認

- **完了日時**: {YYYY-MM-DD HH:MM} ← 完了時に記録
- **Evidence（証拠）**: {テスト結果・ログ・スクショの要約}
- **Recovery（復旧メモ）**: {問題が起きた場合の対処内容 / 問題なし}

---

## フェーズ3: 品質ゲート（DoD 準拠）

> **チェックポイント CP-FINAL**: `.claude/docs/guidelines/definition-of-done.md` の全ゲートをパスするまでクローズしない。
> 最大反復回数: 3回。3回失敗した場合は `.steering/[タスク名]/failure-report.md` を生成してエスカレーション。

### Gate 1: 静的解析（必須）

- [ ] lint が通ること
  - **Validation**: `npm run lint`
  - **受け入れ条件**: エラー 0 件
- [ ] 型チェックが通ること
  - **Validation**: `npm run typecheck`
  - **受け入れ条件**: エラー 0 件
- [ ] 全テストがパスすること
  - **Validation**: `npm test`
  - **受け入れ条件**: 全テストパス
- [ ] ビルドが成功すること
  - **Validation**: `npm run build`
  - **受け入れ条件**: ビルドエラー 0 件

### Gate 2: E2E テスト（設定済みの場合のみ）

- [ ] 全 E2E シナリオがパスすること ← `playwright.config.ts` が存在する場合のみ実行
  - **Validation**: `npm run test:e2e`
  - **受け入れ条件**: 全シナリオパス

### Gate 3: VRT（設定済みの場合のみ）

- [ ] スクリーンショット差分がないこと ← `tests/vrt/__screenshots__/` が存在する場合のみ実行
  - **Validation**: `npm run test:vrt`
  - **受け入れ条件**: 差分 0 件

### Gate 4: a11y（設定済みの場合のみ）

- [ ] WCAG AA 違反がないこと ← `tests/a11y/` が存在する場合のみ実行
  - **Validation**: `npm run test:a11y`
  - **受け入れ条件**: violations 0 件

### Gate 5: UI/UX 批評（設定済みの場合のみ）

- [ ] ui-ux-validator の重大指摘が 0 件 ← `.claude/agents/ui-ux-validator.md` が存在する場合のみ実行

### CP-FINAL チェックポイント確認

- **完了日時**: {YYYY-MM-DD HH:MM} ← 完了時に記録
- **Gate 1 Evidence**: {lint/typecheck/test の結果要約}
- **Gate 2 Evidence**: {E2E 結果 / スキップ（playwright.config.ts なし）}
- **Gate 3 Evidence**: {VRT 結果 / スキップ（基準画像なし）}
- **Gate 4 Evidence**: {a11y 結果 / スキップ（tests/a11y/ なし）}
- **Gate 5 Evidence**: {UI/UX 批評結果 / スキップ（ui-ux-validator.md なし）}
- **反復回数**: {0〜3回}
- **Recovery（復旧メモ）**: {問題が起きた場合の対処内容 / 問題なし}

---

## フェーズ4: ドキュメント更新

- [ ] 影響する `docs/` ドキュメントを更新（必要に応じて）
- [ ] README.md を更新（必要に応じて）
- [ ] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
{YYYY-MM-DD}

### デモ手順
1. `npm run dev` を実行
2. {具体的な操作手順}
3. {期待される結果}

### チェックポイントサマリ

| CP | 完了日時 | 主な問題 | 反復回数 |
|----|---------|---------|---------|
| CP-1 | {日時} | {なし / 問題概要} | {0} |
| CP-2 | {日時} | {なし / 問題概要} | {0} |
| CP-FINAL | {日時} | {なし / 問題概要} | {0} |

### 計画と実績の差分

**計画と異なった点**:
- {計画時には想定していなかった技術的な変更点}
- {実装方針の変更とその理由}

**新たに必要になったタスク**:
- {実装中に追加したタスク}
- {なぜ追加が必要だったか}

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- {タスク名}
  - スキップ理由: {具体的な技術的理由}
  - 代替実装: {何に置き換わったか}

**⚠️ 注意**: 「時間の都合」「難しい」などの理由でスキップしたタスクはここに記載しないこと。全タスク完了が原則。

### 学んだこと

**技術的な学び**:
- {実装を通じて学んだ技術的な知見}
- {新しく使った技術やパターン}

**プロセス上の改善点**:
- {タスク管理で良かった点}
- {ステアリングファイルの活用方法}

### 次回への改善提案
- {次回の機能追加で気をつけること}
- {より効率的な実装方法}
- {タスク計画の改善点}
