# 開発ワークフロー遵守ガードレール

1. `src/` 以下のファイルを作成・編集する前には、`docs/ideas/` 配下に 実装計画 あるいは 仕様書 を作成すること。
2. `/add-feature`、`/add-feature-ui`あるいはそれに準ずるカスタムコマンドを実行して、
`.steering`ディレクトリ配下にステアリングフィル（`requirements.md`、`design.md`、`tasklist.md`）を作成してから実装を開始すること。
